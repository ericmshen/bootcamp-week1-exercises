import React from 'react'
import { SimpleButton } from './styles'

const AddButton = (props) => (
    <SimpleButton type="submit" value="Submit">add todo item</SimpleButton>
)
  
export default AddButton